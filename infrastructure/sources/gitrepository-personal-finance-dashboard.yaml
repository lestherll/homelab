apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: personal-finance-dashboard
  namespace: flux-system
  annotations:
    # See the equivalent annotation on gitrepository-fastapi-echo.yaml for why
    # this is here and why it lands in its own commit. TEMPORARY — strip once
    # the app repo's CI owns the object.
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  interval: 5m
  url: https://github.com/lestherll/personal-finance-dashboard
  ref:
    branch: main

# TEMPLATE — placeholder value, NOT yet encrypted. Before pushing this file for real:
#   1. Pick a real admin password (e.g. `openssl rand -base64 24`).
#   2. Replace CHANGEME below.
#   3. Run: sops --encrypt --in-place infrastructure/observability/grafana-admin-secret.sops.yaml
# Referenced by helmrelease.yaml via grafana.admin.existingSecret.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: observability
type: Opaque
stringData:
  admin-user: admin
  admin-password: CHANGEME-grafana-admin-password
